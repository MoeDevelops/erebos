{
  "scripts": {
    "build": "npx @marp-team/marp-cli --html index.md -o build/index.html --theme theme.css && cp -r assets build"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^4.1.2"
  }
}
